# Core
pandas>=2.1
numpy>=1.26
scikit-learn>=1.3
xgboost>=2.0
joblib>=1.3
requests>=2.31

# Frontend / visualization
streamlit>=1.32
plotly>=5.20
matplotlib>=3.8

# Testing
pytest>=8.0

# Note: if `xgboost` fails to install on your platform (e.g. some ARM/Apple
# Silicon or locked-down environments), the project automatically falls back
# to scikit-learn's HistGradientBoostingClassifier -- everything still works,
# just remove/comment the xgboost line above if installation is problematic.
